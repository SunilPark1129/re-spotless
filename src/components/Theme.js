const defaultTheme = {
    main: "#88B04B",
    constLight: "#dbdbdb",
    constDark: "#111",
    mobile: "760px",
    minWidth: "360px",
    tablet: "80rem",
};

export const lightTheme = {
    colors: {
        light: "#dbdbdb",
        dark: "#222222",
        main: defaultTheme.main,
        constLight: defaultTheme.constLight,
        constDark: defaultTheme.constDark,
    },
    mobile: defaultTheme.mobile,
    minWidth: defaultTheme.minWidth,
    tablet: defaultTheme.tablet,
};

export const darkTheme = {
    colors: {
        light: "#222222",
        dark: "#dbdbdb",
        main: defaultTheme.main,
        constLight: defaultTheme.constLight,
        constDark: defaultTheme.constDark,
    },
    mobile: defaultTheme.mobile,
    minWidth: defaultTheme.minWidth,
    tablet: defaultTheme.tablet,
};